spring:
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowed-origins: "http://195.204.168.195"
            allowed-headers: '*'
            allowed-methods:
              - GET
              - POST
              - DELETE
              - PUT